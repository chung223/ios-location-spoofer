# 从上游 mekos2772 同步更新

本仓库是 [mekos2772/ios-location-spoofer](https://github.com/mekos2772/ios-location-spoofer) 的 fork。新功能通常先进上游，这份文档教你把上游更新合并回来。

## 一次性：加上游 remote

```bash
git remote add upstream https://github.com/mekos2772/ios-location-spoofer.git
git remote -v   # 确认 upstream 存在
```

## 每次手动同步

```bash
git fetch upstream
git checkout main
git merge upstream/main      # 或 git rebase upstream/main
# 有冲突 → 解决后：git add . && git commit（merge）或 git rebase --continue
git push origin main
```

## 冲突提醒

- 你在 fork 里改过的文件（Surge 模块、`location-picker/` 页面、README 等）可能和上游冲突，按 Git 提示逐个解决即可。
- `location-spoofer.js`（核心脚本）建议**尽量跟随上游、少改**，以减少每次同步的冲突面。

## 自动检查（可选）

仓库带一个 GitHub Action：[`.github/workflows/upstream-sync.yml`](.github/workflows/upstream-sync.yml)。

- 触发：可在 **Actions** 页手动 **Run workflow**，或每周一自动跑一次。
- 行为：把 `upstream/main` 合并进一个 `chore/upstream-sync` 分支，**能干净合并**就自动开一个 PR 供你审阅（**不会自动合并**）；**有冲突**就只发一条警告、不开 PR，请改用上面的手动步骤。

## 关于把「别的项目」参考回来合并（许可证）

本仓库是 **AGPL-3.0**。往里合代码时：

- 只能并入与 **AGPL-3.0 兼容**的代码；不确定就**只参考思路、自己重写**（功能/思路不受版权保护，具体代码才受）。
- 保留他人代码的版权声明与许可证文本，遵守其条款。
- 并入后整体仍是 AGPL-3.0，且因为 picker 是网络服务，需对使用者提供源码入口（页面底部已有「源码」链接）。
